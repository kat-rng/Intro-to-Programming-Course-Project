function savePoint(changedID, newX, newY)
    % Applies changes to the point that was made, in turn changing and
    % redrawing both the point and all connected lines.

    global allPoints;
    global allPlottedPoints;
    global allLines;
    global allPlottedLines;
    global editPresent;
    
    editPresent = 0;

end